'''h = MyThread()
h.name = "host-thread"
threads.append(h)
h.start()'''

'''for service in services.services:
    p = Process(target=checkService, args=(service,))
    p.name = service.servicename
    p.start()
    time.sleep(19)
    threads.append(p)'''
    
'''for service in services.services:
    s = MyThread(service)
    s.name = service.servicename
    s.start()
    time.sleep(22)
    threads.append(s)'''
'''for service in services.services:
    checkService(service)'''